import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 10,
    // height: 66,
    // width: 66,
    borderWidth: 3,
    borderColor: '#ae22e0',
    borderRadius: 40,
  },
  image: {
    // height: 60,
    // width: 60,
    borderWidth: 3,
    borderRadius: 35,
    borderColor: '#ffffff',
  },
});

export default styles;
